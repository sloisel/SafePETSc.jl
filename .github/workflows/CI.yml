name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: 'Enable tmate debugging'
        required: false
        default: false

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install MPI
        run: |
          sudo apt-get update
          sudo apt-get install -y mpich libmpich-dev

      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.10'
          arch: x64

      - uses: julia-actions/cache@v2

      - name: Build package
        uses: julia-actions/julia-buildpkg@v1

      - name: Run tests with coverage
        uses: julia-actions/julia-runtest@v1
        with:
          coverage: true

      - name: Setup tmate session for debugging
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event.inputs.debug_enabled == 'true' }}
        timeout-minutes: 30
        with:
          limit-access-to-actor: true

      - name: Process coverage
        uses: julia-actions/julia-processcoverage@v1

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  test-strumpack:
    name: Test with STRUMPACK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      PETSC_RELEASE_TAG: petsc-3.22.0-cuda-20251127

    steps:
      - uses: actions/checkout@v4

      - name: Install MPI
        run: |
          sudo apt-get update
          sudo apt-get install -y mpich libmpich-dev

      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.10'
          arch: x64

      - uses: julia-actions/cache@v2

      - name: Download pre-built PETSc STRUMPACK
        run: |
          echo "Downloading $PETSC_RELEASE_TAG..."
          gh release download $PETSC_RELEASE_TAG --pattern "*.tar.gz" --dir /tmp
          echo "Extracting..."
          mkdir -p /tmp/petsc_strumpack
          # Use --strip-components=1 to remove petsc_strumpack/ prefix from tarball
          tar -xzf /tmp/${PETSC_RELEASE_TAG}.tar.gz -C /tmp/petsc_strumpack --strip-components=1
          echo "Done. Library at /tmp/petsc_strumpack/lib/libpetsc.so"
          ls -la /tmp/petsc_strumpack/lib/
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build package
        uses: julia-actions/julia-buildpkg@v1

      - name: Run tests with STRUMPACK
        env:
          JULIA_PETSC_LIBRARY: /tmp/petsc_strumpack/lib/libpetsc.so
        run: |
          julia --project=. -e 'using Pkg; Pkg.test()'
